/**
 * Search Results Component
 *
 * - Takes in a search query and a list of selected publications search across.
 * - calls an api endpoint to perform google search with site: operator for each selected publication.
 *   - see /api/search for more details
 * - presents the search results in a list format
 *   - search results should be grouped under each publication, which should be a collapsible section.
 *   - it should have a proper loading state while waiting for the api response.
 *   - it should handle and display any errors that occur during the fetch operation.
 */

export default function SearchResults() {
  return <div>Search Results Component</div>;
}
